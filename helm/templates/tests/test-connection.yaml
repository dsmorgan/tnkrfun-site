apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tnkrfun.fullname" . }}-test-connection"
  labels:
    {{- include "tnkrfun.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tnkrfun.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
